/**
 * Main class that creates an instance of the PokemonSelection class and assigns a Pokemon to each player.
 * Group members: David Lee, Rayne Kratz
 */
public class Main {
    public static void main(String[] args) {
        // Create an instance of PokemonSelection.
        PokemonSelection pokemonSelection = new PokemonSelection();

        // Assign a Pokemon to each player.
        pokemonSelection.assignPokemon(1, 2);
    }
}